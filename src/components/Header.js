import Navigation from "./Navigation";
import Logout from "./Logout";

const Header = () => {
  return (
    <header>
      <h1>Keygen UI</h1>
      <Navigation />
      <Logout />
    </header>
  );
};

export default Header;